Clone. Build.

#Build on Windows

Run ```build.cmd```

#Build on Linux

Run ```xbuild build.xml```

#Automatic Build

DojoTimer has automatic build. Access http://juanlopes.net/dojotimer for more info.
